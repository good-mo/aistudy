"""
app —— 金融分析工具箱 · 新一代专业架构
====================================

分层清晰、高复用、可维护的金融数据/分析框架：

    app/
    ├── core/      核心基础设施（配置/日志/缓存/网络/异常）
    ├── data/      统一数据访问层（多数据源+降级链）
    ├── domains/   领域业务模块（基金/沪深300/理财/A股盯盘）
    └── cli/       统一命令行入口与监控调度

设计原则：
    - 依赖倒置：领域模块依赖 data/ 层抽象接口，不直接依赖具体数据源
    - 统一降级：数据源按注册表顺序降级，失败自动切换，保证可用性
    - 统一缓存：分级缓存（内存+磁盘），按数据更新频率设置 TTL
    - 统一异常：DataFetchError 体系，向上抛出可读错误
    - 单一职责：每层只做一件事，模块间松耦合
"""

from app.core.config import AppConfig, get_config

__version__ = "1.0.0"
__all__ = ["AppConfig", "get_config"]
